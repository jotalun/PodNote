{
  "name": "podnote-desktop",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build:pages": "node scripts/build-pages.js",
    "deploy:cloudflare": "npm run build:pages && npx wrangler pages deploy dist",
    "check": "node --check app.js && node --check server.js && node --check functions/api/analyze.js && node --check functions/api/rss.js && node --check functions/api/health.js && node --check scripts/build-pages.js"
  }
}
