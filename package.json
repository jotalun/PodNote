{
  "name": "podnote-desktop",
  "version": "0.15.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:pages",
    "build:pages": "node scripts/build-pages.js",
    "deploy:cloudflare": "npm run build:pages && npx wrangler pages deploy dist",
    "check": "node --check app.js && node --check server.js && node --check lib/auth.js && node --check lib/podcast-source.js && node --check lib/transcript.js && node --check lib/transcribe.js && node --check functions/_middleware.js && node --check functions/api/analyze.js && node --check functions/api/rss.js && node --check functions/api/transcript.js && node --check functions/api/transcribe.js && node --check functions/api/health.js && node --check scripts/build-pages.js"
  }
}
